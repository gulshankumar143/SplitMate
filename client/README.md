# SplitMate Frontend

## Setup

1. Copy `.env.example` to `.env`
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start development server:
   ```bash
   npm run dev
   ```

## Build

```bash
npm run build
```

## Notes

The frontend proxies API requests to the backend at `http://localhost:5000/api`.
